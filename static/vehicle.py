from flask import Flask,request,jsonify,redirect,url_for, make_response
from flask_restful import Resource, Api
from mydbkey import myDb


app = Flask(__name__)

# set database config on app instance
configuration = myDb()
app.config['MYSQL_HOST']=configuration.MYQL_HOST
app.config['MYSQL_USER']=configuration.MYSQL_USER
app.config['MYSQL_PASSWORD']=configuration.MYSQL_PASSWORD
app.config['MYSQL_DB']=configuration.MYSQL_DB



#-------------------The User SignUp page-------------------------------------------#
'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)'''
FOR THE USER SIGNUP
The User info is sent to the to be registered on the database with the password harshed sent to the database

FOR THE USER SIGNIN
The username and password is taken from the form data, hasrhed and sent to be compared with info in the 
database

'''

@app.route('auth/sign_up', methods=['POST'])
def sign_up():
    
        request_data = request.json
        firstname = request.json['firstname']
        lastname = request.json['lastname']
        othernames = request.json['othernames']
        username = request.json['username']
        tel_no = request.json['telephone_number']
        email = request.json['email']
        password = request.json['user_password']
        cur = mysql.connection.cursor()
        cur.execute('''INSERT into User_info (firstname,lastname,othernames,username,telephone_number,
        email,user_password) values(%s,%s,%s,%s,%s,%s,%s)''',(firstname,lastname,othernames,username,tel_no,email,password))
        mysql.connection.commit()
        return jsonify({"User":request_data}),201
        #----------------------- A default Avatar has to be added to the database-----------------------------#


@app.route('auth/sign-in', methods=['POST'])
def sign_in():
    username = request.json['username']
    password = request.json['password']
    cur = mysql.connection.cursor()
    cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
    ''',(username,password))
    rv = cur.fetchall()
    if rv==tuple():
        return redirect(url_for('login'))
    else:
        return str(rv)

    #-------------- This is for the user authentication or login-----------------------#    
   
        #-------------------- This is for the user autentication-------------------------------#
        #-------------------- Or the login ----------------------------------------------------#
        


#---------------------------Login Route----------------------------------------------------------------------#
@app.route('Auth/Login', methods=['POST','PUT'])
def login():
    #-----------------------------This part is for the user authentication-----------------------------------#
    if request.method=='POST':
        username = request.json['username']
        password = request.json['password']
        cur = mysql.connection.cursor()
        cur.execute('''SELECT * FROM User_info WHERE (username=(%s) and user_password=(%s))
        ''',(username,password))
        rv = cur.fetchall()
        if rv==tuple():
            return redirect(url_for('login'))
        else:
            return str(rv)

#--------------------------------------------------------------------------------------------------------------#




#-------------------------------------------------Updating User info-------------------------------------------#
@app.route('/User/<string:username>',methods=['PUT'])    

@app.route('/driver/<string:name>/', methods=['GET','POST','PUT','DEL'])
def driver(name):
    if request.method == 'POST':
        cur = mysql.connection.cursor()
        cur.execute(''' SELECT user_id from User_info WHERE username=%s''',(name,))
        search = fetchone()
        if search==tuple():
            return '<h1>User does not exist</h1>'
        else:
            date_of_birth = request.form['date_of_birth']
            residential_address = request.form['res_add']
            license_no = request.form['license_no']
            sex = request.form['sex']
                #--------- Should validate sex when using text input-------------------------------------------------------------#
                #-----------Driver Photo wil be handled later--------------------------------------------------------------------#
                #-----------Will be whether using blob or vachar-----------------------------------------------------------------#
            cur.execute('''INSERT INTO driver_info (date_of_birth,residential_address,license_no,sex) values(%s,%s,%s,%s)''',(date_of_birth,residential_address,
            license_no,sex))
            mysql.connection.commit()


            


       

if __name__ == "__main__":
    app.run(debug=True)
if __name__ == "__main__":
    app.run(debug=True)